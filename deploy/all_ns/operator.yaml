apiVersion: apps/v1
kind: Deployment
metadata:
  name: zookeeper-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: zookeeper-operator
  template:
    metadata:
      labels:
        name: zookeeper-operator
    spec:
      serviceAccountName: zookeeper-operator
      containers:
        - name: zookeeper-operator
          # Replace this with the built image name
          image: 716309063777.dkr.ecr.us-east-1.amazonaws.com/zookeeper-operator:IN-561-18
          ports:
          - containerPort: 60000
            name: metrics
          command:
          - zookeeper-operator
          args:
          - "--top-domain=imp"
          imagePullPolicy: Always
          env:
          - name: WATCH_NAMESPACE
            value: ""
          - name: POD_NAME
            valueFrom:
              fieldRef:
                fieldPath: metadata.name
          - name: OPERATOR_NAME
            value: "zookeeper-operator"
